
server:
  port: 8080
spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    defer-datasource-initialization: true

  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace



jwt:
  issuer: jara
  subject: auth
  expires-time: 3600 # 3600 1시간
  secret: my-secret